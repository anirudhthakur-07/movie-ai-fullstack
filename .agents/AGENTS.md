# Rules

- **Git Commit Workflow:** Do not display raw git terminal commands in chat responses. Always execute git commits and pushes automatically using the terminal tools directly on behalf of the user.
